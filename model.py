import numpy as np
import matplotlib.pyplot as plt
from sklearn.datasets import make_blobs

def Initialisation(X):

def Model(X, W, b):

def Cost(A, y)
    
def Gradients(A, X, y)
    
Update(W, b, dW, db)